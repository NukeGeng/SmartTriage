# SmartTriage Frontend

Next.js App Router frontend for SmartTriage.

## Local Development

```bash
npm install
npm run dev
```

The app runs at `http://localhost:3000` and calls the backend from `NEXT_PUBLIC_BACKEND_URL`.
